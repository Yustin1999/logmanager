

export default function Restore() {
    
    
    return (
        //Link to a and change a 0 to a 1
        <div>
        <h1>This page it to try and mirror what is happening when unarchiving a user using the DB</h1>

        </div>

    )
}